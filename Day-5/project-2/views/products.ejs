<!-- Route: /products
Data: Array of objects → { name, price }
Render: Loop in EJS to show cards
Tailwind: Grid layout, spacing, image styling, hover effects -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="text-center grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
        <% products.forEach((val)=>{%>
            <div class="bg-white shadow-md p-6 rounded-lg">
            <p><%=val.name%></p>
            <p><%=val.price%></p>
            </div>
        <%})%>
    </div>
</body>
</html>